# Data Analysis Example

This repository contains a simple Python script `analyze_data.py` that reads a CSV file and prints basic summary statistics using `pandas`.

## Requirements

- Python 3
- pandas (install via `pip install pandas`)

## Usage

Run the script and provide the path to a CSV file:

```bash
python analyze_data.py path/to/your/data.csv
```
